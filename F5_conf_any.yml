---

- name: Save and restore configuration
  hosts: all
  connection: local

  tasks:
    - name: Reset the BIG-IP configuration, for example, to RMA the device
      bigip_config:
        reset: yes
        save: yes
        provider:
          server: 10.138.0.2
          password: Nomios101
          user: admin

    - name: Load an SCF configuration
      bigip_config:
        merge_content: "{{ lookup('file', '/path/to/azzedeen.scf') }}"
        provider:
          server: 10.138.0.2
          password: Nomios101
          user: admin
   
  
